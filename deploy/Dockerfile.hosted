# InferSight hosted tier (ingest + dashboard + alerting)
FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml README.md ./
COPY infersight/ infersight/
COPY hosted/ hosted/
RUN pip install --no-cache-dir .
EXPOSE 9000
CMD ["uvicorn", "hosted.ingest:app", "--host", "0.0.0.0", "--port", "9000"]
